#playing with the tuples.
#small file just for practice.

names = ("Roger", "Syd") #tuple

l = len(names)
i =names.index("Roger")

newTupple = names + ("Tina", "Grayson") #adding in the tuple. Cannot add in the original tuple

print(names, i, l, newTupple)